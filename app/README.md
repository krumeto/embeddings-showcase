# Streamlit app

This folder is reserved for the future Streamlit dashboard.

The first implementation pass intentionally prepares the data, embedding, similarity, clustering, and projection helpers without building the dashboard UI yet.
